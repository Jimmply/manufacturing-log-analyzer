# Core
pandas>=2.0.0
numpy>=1.24.0
streamlit>=1.30.0
plotly>=5.18.0
pytest>=7.4.0

# LLM providers — install the one(s) you want to use:
google-genai>=1.0.0     # Gemini (free) — recommended to start
openai>=1.0.0           # OpenAI, Groq, or Ollama (all use OpenAI SDK)
# anthropic>=0.40.0     # Anthropic Claude (paid) — uncomment if needed
